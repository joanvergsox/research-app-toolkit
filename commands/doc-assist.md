---
name: ra:doc-assist
description: Assist with application documents (SOP, motivation letter, research proposal, recommendation letter)
argument-hint: "[sop|motivation|rp|referee]"
---

# Document Assist Command

Parse the document type from `$ARGUMENTS`. Supported types: `sop`, `motivation`, `rp`, `referee`. Default to `sop` if not specified.

Follow the document-assist skill for full writing process.
